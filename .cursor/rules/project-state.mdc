---
description: Overall project state - social media agents, backend, and pending tasks
alwaysApply: true
---

# PalmCare AI — Project State (March 2026)

## Apple Subscriptions (Jul 10, 2026)
- **DO NOT SUBMIT v1.1 to App Review** until the user gives an explicit green light. The earlier review submission was cancelled (v1.1 state: DEVELOPER_REJECTED); there are no active review submissions.
- Final pricing (group 22222636, all set in all 175 territories, Enterprise = `pro` product IDs): Starter $199/mo + $1,899.99/yr, Growth $699/mo + $6,699.99/yr, Enterprise $1,199.99/mo + $10,000/yr (Apple's absolute max — works out to ~30% off; $11,520 was impossible). 14-day free trial on Starter + Growth (monthly AND annual) via introductory offers; no trial on Enterprise. All 6 subscriptions are READY_TO_SUBMIT / WAITING_FOR_REVIEW.
- Product IDs: `com.palmcareai.app.{starter,growth,pro}.{monthly,annual}`.
- **Higher price points APPROVED** (Jul 10) — catalog now goes to $10,000. Note: available points are like $1,199.99/$1,899.99/$6,699.99, not round numbers.
- ASC scripting: use the **official API** (`api.appstoreconnect.apple.com`) with JWT from `~/.appstoreconnect/private_keys/AuthKey_N5LFW2CYVG.p8` — see `scripts/asc/asc_api.py` (needs `python3.11`, PyJWT). Worldwide repricing pattern: find USA price point → GET its `equalizations` → POST `subscriptionPrices` per territory (`scripts/asc/set_subscription_prices.py`). If a sub is stuck in MISSING_METADATA after pricing, PATCH its `reviewNote` to trigger state recompute.
- In-app refund requests implemented (`beginRefundRequest(in:)` in SettingsView) — was required for the higher price points form.
- Per-tier AI rate limits: `apps/api/app/core/tier_limits.py` (free 5, starter 15, growth 40, enterprise 120 req/min) enforced on pipeline + uploads routers.
- iOS: `StoreKitService.swift` (StoreKit 2) + `PaywallView.swift`, entry via Settings → Your Plan. Backend verify endpoint: `POST /billing/apple/verify`; server notifications: `POST /billing/apple/notifications`.
- Apple root CAs bundled at `apps/api/certs/apple/` (loader falls back to this path; `APPLE_ROOT_CERTS_DIR` overrides).
- Upload signing: App Store Connect API key `AuthKey_N5LFW2CYVG.p8`, issuer `18430c66-4e0a-4733-8058-efaef5e61ce0` — use with `xcodebuild -exportArchive -authenticationKeyID N5LFW2CYVG ...` for cloud signing (local keychain has no distribution cert).

## App Launch + ASO (Jul 10, 2026)
- **App is LIVE on the App Store** (v1.0 READY_FOR_SALE): https://apps.apple.com/us/app/palm-home-care-contracts/id6766371988
- **New ASO metadata STAGED on v1.1** (goes live when v1.1 is approved — still awaiting user green light to submit): title `PALM: Home Health Care Agency`, subtitle `AI Notes, Billing & Contracts`, keyword field `caregiver,nurse,aide,hha,private,duty,visit,documentation,soap,intake,transcription,scheduling`, plus new promotional text. Full rationale in `marketing/appstore-metadata.md`.
- Launch emails sent Jul 10 to 8 recipients (Fortcom/Shola signed up Jul 10, Robin Phillips, shwedone4, Family Love Behavioral/Nurdennis, AccentCare/Ken, Mission HHA/Heather, Covenant/Patricia, Prime Care/Mona) via `scripts/email/send_app_launch_emails.py` (brochure PDF attached, App Store CTA).
- Launch assets hosted: `palmcareai.com/launch/palm-app-launch.mp4` (7s launch video) + `palm-launch-email-hero.png` (email banner).

## TestFlight Release Checklist (do ALL of these on EVERY build)
App ID `6766371988` ("PALM – Home Care Contracts"), Team `BM5U63WP92`. App Store Connect internal API base: `https://appstoreconnect.apple.com/iris/v1`.
1. Bump `CURRENT_PROJECT_VERSION` in `ios-app/project.yml`, archive, export+upload.
2. Wait for build `processingState` to be `VALID`.
3. **Set "What to Test"** on the build's `betaBuildLocalizations` — NOTE: the iris API field is `whatsNew` (NOT `whatToTest`). Describe exactly what changed and the steps to test it.
4. Delete any stale `betaAppReviewSubmissions` from older builds, then POST a new one for the new build.
5. Attach the build to the **Agencies** external group (`5cac674b-59ab-4c60-95e1-3ee557c47c86`). Internal group is `95481cc3-2538-41e6-a4b3-83e6b7e3cafa` (can't attach builds to internal group via API).
6. Confirm review-detail demo account is the WORKING one: **`demo-screenshots@palmtai.com`** — password is in `.env` as `DEMO_ACCOUNT_PASSWORD` (`demoAccountRequired: true`). NEVER write the password in tracked files (this repo is public; the old value that leaked here must be rotated). The old `museibrahim@palmtai.com` account is BROKEN (401) — never use it.
7. External testers must be **INVITED** (POST `betaTesterInvitations`) — adding them to a group does NOT send the invite. `info@patronseniorliving.com` (id `28eae6dc-6d92-482b-963c-23afbb855a12`) is in the Agencies group.

## Social Media Accounts
- **LinkedIn**: RE-AUTHED 2026-07-09 to the CORRECT account — **Muse Ibrahim**, member `sub` `_Q02J32AXf` (linked to the Palm Technologies company page). App "PalmcareAi" client ID `86gmdw2m7aqp7g` (verified with Palm Technologies); member token (`openid profile w_member_social`, 60-day) in `.env` as `LINKEDIN_ACCESS_TOKEN`, `LINKEDIN_PERSON_ID=_Q02J32AXf`. Re-auth flow: OAuth authorize URL with redirect `https://www.linkedin.com/developers/tools/oauth/redirect`, grab `code`, exchange at `/oauth/v2/accessToken`. ⚠️ An OLD wrong personal account (`sub` `dGjGZTEMkJ`) was used by mistake and has since been deleted by the user — never use it.
- **Facebook + Instagram**: Meta app "PALM Social Publisher" (`1572111384543106`). Page + IG business tokens in `.env` (`META_PAGE_ID/META_PAGE_ACCESS_TOKEN/META_IG_BUSINESS_ID`).
- **Threads**: WORKING 2026-07-09. Threads app `5302490696643280`, account `@palmcareai` (user id `27968324992825390`). Long-lived token (60-day) in `.env` as `META_THREADS_USER_TOKEN`. Token generated via Meta dev dashboard "User Token Generator" (redirect to `palmcareai.com/threads/callback` on the OAuth authorize path is NOT whitelisted / Save silently fails — instead use the in-dashboard **Generate Access Token** button, approve the Threads consent popup, then read the long-lived `TH...` token from the token_generator popup's page HTML and `th_refresh_token` it).
- **Twitter/X**: API key/secret in `.env`, access token/secret still needed.
- Note: The local `aitask` daemon + email/social subagent scripts were removed on 2026-05-19. All outreach is now manual or backend-driven.

## Automated Social Posting (Jul 13 – Aug 9 plan)
- Social posting is FULLY CLOUD-BASED (no local machine needed), set up 2026-07-09:
  - **Facebook**: all 13 posts natively scheduled inside Meta Business Suite via `scripts/social/schedule_meta_fb.py` (11:30 AM local, log in `scripts/social/.scheduled_fb.json`).
  - **IG + Threads (Mon/Wed/Fri) and LinkedIn (Tue/Thu)**: GitHub Actions workflow `.github/workflows/social-posts.yml` runs `run_scheduled_posts.py` daily at 15:30 UTC (11:30 AM ET). Secrets (META_*, LINKEDIN_*) are set on the `museibrahim1/homecare-ai-mvp` repo. The workflow commits `.posted_log.json` back to main as a double-post guard. Manual trigger: `gh workflow run social-posts.yml -f date=YYYY-MM-DD -f dry_run=true`.
  - The old local launchd agent `com.palmcareai.socialposts` was REMOVED (plist deleted) — don't recreate it.
  - Threads caps captions at 500 chars; the runner auto-trims via `threads_safe()`.
  - ⚠️ Tokens expire ~Sep 8, 2026 (LinkedIn + Threads 60-day). Refresh and update BOTH `.env` and the GitHub repo secrets.
- Posters: `scripts/social/post_to_meta.py` (fb/ig/th) and `scripts/social/post_to_linkedin.py` (image/document + first comment). Dedupe via `scripts/social/.posted_log.json`.
- Creatives + captions: `marketing/content-calendar-month-july.md`; assets in `apps/web/public/marketing/social/` (served at `palmcareai.com/marketing/social/`).

## Backend API
- FastAPI on Railway: `https://api-production-a0a2.up.railway.app`
- Deepgram Nova-3 for transcription, Claude Sonnet 4.6 (`claude-sonnet-4-6`) for LLM
- Contract generation uses 50-state rules from `apps/worker/libs/state_rules.py`
- Pipeline: transcribe → speakers → billables → notes → contract

## Marketing Website State (Feb 22)
- `/features` page: 3 sections (AI Intelligence, Mobile App, Billing & Analytics) with real screenshots
- `/mobile-app` page: PhoneFrame component with actual iOS screenshots (no more React mock UI)
- Nav: Resources dropdown has Contact Us, System Status, Privacy Policy (Help Center removed — it's auth-only)
- Contact page: email only (sales@palmtai.com), no phone number
- Screenshots live in `apps/web/public/screenshots/` and `ios/` subfolder

## App Store Screenshots & Demo Data (Jun 9, 2026)
- **App Store set**: `marketing/app-store-screenshots/` — 19 captures of every screen at 6.9" size (1320×2868), iPhone 17 Pro Max sim, status bar 9:41. Use for App Store listing + onboarding brochure.
- **Web set**: `apps/web/public/screenshots/ios/` replaced with rebranded PALM captures (old deleted, recording shots kept).
- **Demo account (production)**: `demo-screenshots@palmtai.com` — "Sunrise Home Care" (FL), 5 realistic clients, 1 fully processed assessment (Eleanor Whitfield: transcript, 8 billables, SOAP notes, contract).
- **Capture method**: `idb ui tap` (point coords, 440×956) — cliclick is unreliable near the Simulator window top. Auto-login via `SIMCTL_CHILD_AUTOMATION_LOGIN_EMAIL/PASSWORD` env vars (DEBUG builds).
- **State readiness**: `scripts/test_all_states.py` validates all 51 jurisdictions structurally; `--llm` runs real contract analysis per state. Last run: 51/51 passed.

## Pending Tasks
1. LinkedIn: Re-authorize with `openid profile w_member_social` scopes (Share on LinkedIn product now enabled)
2. Facebook: Create Meta Developer App, get Page Access Token with `pages_manage_posts`
3. Instagram: Convert `@palmcareai` to Business account, link to Facebook Page
4. ~~Verify `palmtai.com` domain DNS on Resend~~ — `send.palmtai.com` is VERIFIED and working. Root `palmtai.com` re-verification triggered.
5. Apple Developer Program enrollment (business account `museibrahim@palmtai.com`)

## Environment
- `.env` has all API keys — NEVER commit secrets or show them in responses
- Email: Resend API (`re_fYB7LY6X_...`), domain `send.palmtai.com` VERIFIED, `palmtai.com` failed verification. Use `sales@send.palmtai.com` as sender.
- Railway RESEND_API_KEY may be expired — needs refresh via Railway dashboard
- User emails: `musajama89@gmail.com` (personal), `museibrahim@palmtai.com` (business)
