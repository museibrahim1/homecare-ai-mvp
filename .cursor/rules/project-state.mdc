---
description: Overall project state - social media agents, backend, and pending tasks
alwaysApply: true
---

# PalmCare AI — Project State (March 2026)

## TestFlight Release Checklist (do ALL of these on EVERY build)
App ID `6766371988` ("PALM – Home Care Contracts"), Team `BM5U63WP92`. App Store Connect internal API base: `https://appstoreconnect.apple.com/iris/v1`.
1. Bump `CURRENT_PROJECT_VERSION` in `ios-app/project.yml`, archive, export+upload.
2. Wait for build `processingState` to be `VALID`.
3. **Set "What to Test"** on the build's `betaBuildLocalizations` — NOTE: the iris API field is `whatsNew` (NOT `whatToTest`). Describe exactly what changed and the steps to test it.
4. Delete any stale `betaAppReviewSubmissions` from older builds, then POST a new one for the new build.
5. Attach the build to the **Agencies** external group (`5cac674b-59ab-4c60-95e1-3ee557c47c86`). Internal group is `95481cc3-2538-41e6-a4b3-83e6b7e3cafa` (can't attach builds to internal group via API).
6. Confirm review-detail demo account is the WORKING one: **`demo-screenshots@palmtai.com` / `ScreenShot2026!Demo`** (`demoAccountRequired: true`). The old `museibrahim@palmtai.com / HomeHealthCareAi13` is BROKEN (401) — never use it.
7. External testers must be **INVITED** (POST `betaTesterInvitations`) — adding them to a group does NOT send the invite. `info@patronseniorliving.com` (id `28eae6dc-6d92-482b-963c-23afbb855a12`) is in the Agencies group.

## Social Media Accounts
- **LinkedIn**: RE-AUTHED 2026-07-09. App "PalmcareAi" client ID `86gmdw2m7aqp7g`; member token (`w_member_social`, 60-day, expires ~Sep 8) in `.env` as `LINKEDIN_ACCESS_TOKEN`, Person `sub` `dGjGZTEMkJ` (`LINKEDIN_PERSON_ID`). Re-auth flow: OAuth authorize URL with redirect `https://www.linkedin.com/developers/tools/oauth/redirect`, grab `code`, exchange at `/oauth/v2/accessToken`.
- **Facebook + Instagram**: Meta app "PALM Social Publisher" (`1572111384543106`). Page + IG business tokens in `.env` (`META_PAGE_ID/META_PAGE_ACCESS_TOKEN/META_IG_BUSINESS_ID`).
- **Threads**: WORKING 2026-07-09. Threads app `5302490696643280`, account `@palmcareai` (user id `27968324992825390`). Long-lived token (60-day) in `.env` as `META_THREADS_USER_TOKEN`. Token generated via Meta dev dashboard "User Token Generator" (redirect to `palmcareai.com/threads/callback` on the OAuth authorize path is NOT whitelisted / Save silently fails — instead use the in-dashboard **Generate Access Token** button, approve the Threads consent popup, then read the long-lived `TH...` token from the token_generator popup's page HTML and `th_refresh_token` it).
- **Twitter/X**: API key/secret in `.env`, access token/secret still needed.
- Note: The local `aitask` daemon + email/social subagent scripts were removed on 2026-05-19. All outreach is now manual or backend-driven.

## Automated Social Posting (Jul 13 – Aug 9 plan)
- `scripts/social/run_scheduled_posts.py` runs daily 11:30 AM via launchd `com.palmcareai.socialposts`. Meta days (Mon/Wed/Fri) → FB + IG + Threads (link in caption). LinkedIn days (Tue/Thu) → image or PDF document carousel, signup link posted as the FIRST COMMENT.
- Posters: `scripts/social/post_to_meta.py` (fb/ig/th) and `scripts/social/post_to_linkedin.py` (image/document + first comment). Dedupe via `scripts/social/.posted_log.json`.
- Creatives + captions: `marketing/content-calendar-month-july.md`; assets in `apps/web/public/marketing/social/` (served at `palmcareai.com/marketing/social/`).

## Backend API
- FastAPI on Railway: `https://api-production-a0a2.up.railway.app`
- Deepgram Nova-3 for transcription, Claude Sonnet 4.6 (`claude-sonnet-4-6`) for LLM
- Contract generation uses 50-state rules from `apps/worker/libs/state_rules.py`
- Pipeline: transcribe → speakers → billables → notes → contract

## Marketing Website State (Feb 22)
- `/features` page: 3 sections (AI Intelligence, Mobile App, Billing & Analytics) with real screenshots
- `/mobile-app` page: PhoneFrame component with actual iOS screenshots (no more React mock UI)
- Nav: Resources dropdown has Contact Us, System Status, Privacy Policy (Help Center removed — it's auth-only)
- Contact page: email only (sales@palmtai.com), no phone number
- Screenshots live in `apps/web/public/screenshots/` and `ios/` subfolder

## App Store Screenshots & Demo Data (Jun 9, 2026)
- **App Store set**: `marketing/app-store-screenshots/` — 19 captures of every screen at 6.9" size (1320×2868), iPhone 17 Pro Max sim, status bar 9:41. Use for App Store listing + onboarding brochure.
- **Web set**: `apps/web/public/screenshots/ios/` replaced with rebranded PALM captures (old deleted, recording shots kept).
- **Demo account (production)**: `demo-screenshots@palmtai.com` — "Sunrise Home Care" (FL), 5 realistic clients, 1 fully processed assessment (Eleanor Whitfield: transcript, 8 billables, SOAP notes, contract).
- **Capture method**: `idb ui tap` (point coords, 440×956) — cliclick is unreliable near the Simulator window top. Auto-login via `SIMCTL_CHILD_AUTOMATION_LOGIN_EMAIL/PASSWORD` env vars (DEBUG builds).
- **State readiness**: `scripts/test_all_states.py` validates all 51 jurisdictions structurally; `--llm` runs real contract analysis per state. Last run: 51/51 passed.

## Pending Tasks
1. LinkedIn: Re-authorize with `openid profile w_member_social` scopes (Share on LinkedIn product now enabled)
2. Facebook: Create Meta Developer App, get Page Access Token with `pages_manage_posts`
3. Instagram: Convert `@palmcareai` to Business account, link to Facebook Page
4. ~~Verify `palmtai.com` domain DNS on Resend~~ — `send.palmtai.com` is VERIFIED and working. Root `palmtai.com` re-verification triggered.
5. Apple Developer Program enrollment (business account `museibrahim@palmtai.com`)

## Environment
- `.env` has all API keys — NEVER commit secrets or show them in responses
- Email: Resend API (`re_fYB7LY6X_...`), domain `send.palmtai.com` VERIFIED, `palmtai.com` failed verification. Use `sales@send.palmtai.com` as sender.
- Railway RESEND_API_KEY may be expired — needs refresh via Railway dashboard
- User emails: `musajama89@gmail.com` (personal), `museibrahim@palmtai.com` (business)
