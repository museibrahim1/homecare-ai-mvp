/**
 * Push notifications stub.
 * expo-notifications removed for personal dev team compatibility.
 */

export async function registerForPushNotifications(): Promise<string | null> {
  return null;
}
