# Celery and Redis
celery[redis]>=5.3.0
redis>=5.0.0

# Database
sqlalchemy>=2.0.25
psycopg[binary]>=3.1.0

# S3/MinIO
boto3>=1.34.0

# ASR - faster-whisper
faster-whisper>=1.0.0

# Diarization - pyannote
# Pin versions for compatibility (AudioMetaData and use_auth_token fix)
pyannote.audio>=3.1.0,<3.2.0
torch>=2.0.0,<2.2.0
torchaudio>=2.0.0,<2.2.0
# Pin huggingface_hub to version that supports use_auth_token parameter
huggingface_hub>=0.19.0,<0.21.0

# Audio processing
pydub>=0.25.1
soundfile>=0.12.0
# Pin numpy to 1.x for pyannote.audio compatibility
numpy>=1.24.0,<2.0.0

# Settings
pydantic-settings>=2.1.0

# Document generation
python-docx>=1.1.0

# LLM - OpenAI and Anthropic (Claude)
openai>=1.12.0
anthropic>=0.18.0
